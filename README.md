# HyprGroup

HyprGroup is a small Hyprland helper for controlling native groups like
a focused set of tabs. It keeps Hyprland's own group model as the source
of truth and adds command and menu affordances around it.

The project is intentionally relocatable: Hyprland loads one Lua entry
file, and the helper resolves the rest of the repository from there.
